<% set_page_title("#{t('form.submitted.title')} - #{@current_context.form_name}") %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= govuk_panel(title_text: t('form.submitted.title')) %>
  </div>
</div>
