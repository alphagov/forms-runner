<% set_page_title(t("maintenance.title")) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-l"><%= t('maintenance.title') %></h1>
    <%= simple_format(t('maintenance.body'), class: 'govuk-body') %>
  </div>
</div>
