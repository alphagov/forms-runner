---
applications:
- name: forms-runner-dev
  memory: 256M
  command: bin/rails server
  services:
    - forms-runner-dev-redis
  env:
    RAILS_ENV: production
    RACK_ENV: production
    API_BASE: ((API_BASE))
    NOTIFY_API_KEY: ((NOTIFY_API_KEY))
